<Window x:Class="PrismWpf.Views.AnotherWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PrismWpf.Views"
        mc:Ignorable="d"
        Title="AnotherWindow" Height="450" Width="800">
    <Grid>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <CheckBox IsChecked="{Binding IsEnabled}" x:Name="chk" Content="Can Execute Command" Margin="10" />
            <Button Command="{Binding ButtonCommand}" Content="My command" Margin="10" />
            <TextBlock Text="{Binding UpdateText}" Margin="10" FontSize="18" />
        </StackPanel>
    </Grid>
</Window>
